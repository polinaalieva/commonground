import React from 'react'
import ReactDOM from 'react-dom/client'
import DrawPage from './DrawPage'

ReactDOM.createRoot(document.getElementById('draw-root')).render(
  <React.StrictMode>
    <DrawPage />
  </React.StrictMode>
)